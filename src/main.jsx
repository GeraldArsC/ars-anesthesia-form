import React from "react";
import ReactDOM from "react-dom/client";
import ArsAnesthesiaForm from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ArsAnesthesiaForm />
  </React.StrictMode>
);